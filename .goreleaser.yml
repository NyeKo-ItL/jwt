version: 2

# This repository is a library. GoReleaser publishes the GitHub release and
# source archive without building command binaries.
builds:
  - id: library
    skip: true


checksum:
  name_template: "checksums.txt"
